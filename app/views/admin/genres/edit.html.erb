<h1>ジャンル編集</h1>

<%= form_with model: @editgenre, :url => {:action => :update}, method: :patch do |f| %>

  <div class = "">
    <label for = "">ジャンル名</label>
    <%= f.text_field :name %>
  </div>

  <div class="">
    <%= f.submit "変更を保存" %>
  </div>

<% end %>